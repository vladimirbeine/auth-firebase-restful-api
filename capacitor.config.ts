import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'user-auth-firebase-restful',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
